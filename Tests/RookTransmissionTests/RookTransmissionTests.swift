import XCTest
@testable import RookTransmission

final class RookTransmissionTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RookTransmission().text, "Hello, World!")
    }
}
